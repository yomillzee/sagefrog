<!--
House style: write the body as prose, not a checklist. Two or three short
paragraphs. Lead with what a person using the portal was living with, then
what changed, then anything that needed a judgement call. No file paths, no
function names -- those are in the diff. Delete this comment when you're done.
-->

## What changed and why



## Notes

<!--
Optional. Only what a reviewer can't get from the diff: a trade-off you
weighed, a migration, a fallback kept for an existing Railway variable, a
deliberate non-change.
-->

---

<!-- Delete any line that doesn't apply. -->

- Changelog: `railway/app/changelog.py` entry added (user-visible change) / not needed (invisible to anyone using the portal)
- Verified: renderer rendered for real / `python -m pytest tests/<file>` from `railway/app` / n/a
